import { useSelector } from "react-redux";

const Home = () =>{

    const UserDetails = useSelector((store) => store.Login);
    
    return(
        <>
       
        
        </>
    )
}
export default Home;